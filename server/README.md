# Mig backend

Set variables on Railway:

```text
NODE_ENV=production
OWNER_TOKEN=<strong-secret-token>
DATA_DIR=/data
PUBLIC_URL=https://your-app.up.railway.app
```

Run locally:

```bash
npm install
npm start
```
