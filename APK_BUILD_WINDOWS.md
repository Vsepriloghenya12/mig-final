# APK build

1. Запустите backend: `run-backend-windows.cmd`.
2. Соберите APK: `build-apk-windows.cmd`.
3. Установите APK на телефон.
4. В приложении введите LAN-адрес backend, который показан в терминале.
